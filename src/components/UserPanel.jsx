import React from 'react';

const UserPanel = () => {
  return (
    <div>
      {/* Add components for Mark Attendance, Mark Leave, View Attendance here */}
    </div>
  );
};

export default UserPanel;
